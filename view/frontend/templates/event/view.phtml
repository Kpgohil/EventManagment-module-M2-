<?php
/**
 * @var \Elsnertech\Event\Block\EventView $block
 */
$event = $block->getEvent();
if (!$event) {
    return;
}

$title = (string)$event->getData('title');
$venue = (string)$event->getData('venue');
$shortDesc = (string)$event->getData('short_description');
$description = $event->getData('description');
$startDt = $event->getData('start_datetime');
$endDt = $event->getData('end_datetime');
$featuredImg = $block->getFeaturedImage();
$gallery = $block->getGalleryImages();
$hasGallery = !empty($gallery);
$venueLabel = $block->getDefaultVenueLabel();
?>

<div class="els-view">

    <!-- Hero Banner -->
    <div class="els-view__hero" <?php if ($featuredImg): ?>style="background-image: url('<?= $block->escapeUrl($featuredImg) ?>')"<?php endif; ?>>
        <div class="els-view__hero-overlay"></div>
        <div class="els-view__hero-content">

            <!-- Breadcrumb -->
            <a href="<?= $block->escapeUrl($block->getUrl('events')) ?>" class="els-view__back">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/>
                </svg>
                <?= $block->escapeHtml(__('All Events')) ?>
            </a>

            <!-- Date Badge -->
            <div class="els-view__date-badge">
                <span class="els-view__date-day"><?= $block->escapeHtml($block->getDay($startDt)) ?></span>
                <span class="els-view__date-month"><?= $block->escapeHtml($block->getMonth($startDt)) ?></span>
                <span class="els-view__date-year"><?= $block->escapeHtml($block->getYear($startDt)) ?></span>
            </div>

            <h1 class="els-view__title"><?= $block->escapeHtml($title) ?></h1>

            <div class="els-view__hero-meta">
                <?php 
                    $displayVenue = $venue;
                    if ($displayVenue === '' && $venueLabel !== '') {
                        $displayVenue = $venueLabel;
                    }
                ?>
                <?php if ($displayVenue): ?>
                    <span class="els-view__hero-meta-item">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
                        </svg>
                        <?= $block->escapeHtml($displayVenue) ?>
                    </span>
                <?php endif; ?>
                <span class="els-view__hero-meta-item">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
                    </svg>
                    <?= $block->escapeHtml($block->getFormattedDate($startDt)) ?>
                </span>
                <span class="els-view__hero-meta-item">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
                    </svg>
                    <?= $block->escapeHtml($block->getTimeRange()) ?>
                </span>
            </div>

        </div>
    </div>

    <div class="els-view__body">

        <!-- Sidebar + Main Layout -->
        <div class="els-view__layout">

            <!-- Main Content -->
            <main class="els-view__main">

                <?php if ($shortDesc): ?>
                    <section class="els-view__section els-view__short">
                        <p><?= $block->escapeHtml($shortDesc) ?></p>
                    </section>
                <?php endif; ?>

                <?php if ($description): ?>
                    <section class="els-view__section els-view__description">
                        <h2 class="els-view__section-title"><?= $block->escapeHtml(__('About This Event')) ?></h2>
                        <div class="els-view__desc-content"><?= /* @noEscape */ $description ?></div>
                    </section>
                <?php endif; ?>

                    <?php if ($block->isShowGalleryEnabled() && $hasGallery): ?>
                    <section class="els-view__section els-view__gallery-section">
                        <h2 class="els-view__section-title"><?= $block->escapeHtml(__('Gallery')) ?></h2>
                        <div class="els-view__gallery">
                            <?php foreach ($gallery as $image): ?>
                                <a href="<?= $block->escapeUrl($image['url']) ?>" class="els-view__gallery-item" target="_blank">
                                    <img src="<?= $block->escapeUrl($image['url']) ?>"
                                         alt="<?= $block->escapeHtml($image['label'] ?: $title) ?>"
                                         loading="lazy">
                                </a>
                            <?php endforeach; ?>
                        </div>
                    </section>
                    <?php endif; ?>

            </main>

            <!-- Sidebar -->
            <aside class="els-view__sidebar">

                <div class="els-view__sidebar-card">
                    <h3 class="els-view__sidebar-title"><?= $block->escapeHtml(__('Event Details')) ?></h3>

                    <dl class="els-view__details">
                        <div class="els-view__details-row">
                            <dt>
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
                                </svg>
                                <?= $block->escapeHtml(__('Date')) ?>
                            </dt>
                            <dd><?= $block->escapeHtml($block->getFormattedDate($startDt)) ?></dd>
                        </div>

                        <?php if ($endDt): ?>
                        <div class="els-view__details-row">
                            <dt>
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
                                </svg>
                                <?= $block->escapeHtml(__('End Date')) ?>
                            </dt>
                            <dd><?= $block->escapeHtml($block->getFormattedDate($endDt)) ?></dd>
                        </div>
                        <?php endif; ?>

                        <div class="els-view__details-row">
                            <dt>
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
                                </svg>
                                <?= $block->escapeHtml(__('Time')) ?>
                            </dt>
                            <dd><?= $block->escapeHtml($block->getTimeRange()) ?></dd>
                        </div>

                        <?php if ($displayVenue): ?>
                        <div class="els-view__details-row">
                            <dt>
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
                                </svg>
                                <?= $block->escapeHtml(__('Venue')) ?>
                            </dt>
                            <dd><?= $block->escapeHtml($displayVenue) ?></dd>
                        </div>
                        <?php endif; ?>
                    </dl>
                </div>

                <div class="els-view__sidebar-card els-view__sidebar-card--cta">
                    <p><?= $block->escapeHtml(__('Don\'t miss out on this event!')) ?></p>
                    <a href="<?= $block->escapeUrl($block->getUrl('events')) ?>" class="els-view__btn">
                        <?= $block->escapeHtml(__('Browse All Events')) ?>
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
                        </svg>
                    </a>
                </div>

            </aside>
        </div>
    </div>

</div>
